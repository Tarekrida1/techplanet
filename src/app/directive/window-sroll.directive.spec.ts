import { WindowSrollDirective } from './window-sroll.directive';

describe('WindowSrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowSrollDirective();
    expect(directive).toBeTruthy();
  });
});
